1)JavaScript is a programming language that adds interactivity to your website.
-> behaviour reposnes when buttons are pressed in games
->data entry forms
->dynamic Styling
->also with the animation
2)It is invented by Brendan Eich.
3)Versatile and beginner friendly.
4)Used to build 
->database-driven apps
->animating 2D and 3D graphics
->create games
what is let keyword?
The 'let' keyword is use to declare a block scoped variable.
The variables that are declared with 'let' are only accessible within the block,statement, or expression where they are declared.
that means they are not accessible outside of the at specified scope.
Example: 
let x=10;
if(true){ // this is an if statement with a condition that is always true.
    let y=20;
    console.log(x); //output: 10
    console.log(y); //output: 20
}
console.log(x); //output: 10
console.log(y); //error: y is not defined
*Variables*
i)Variables are containers that stores values.
ii)



